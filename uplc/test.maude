load loads.maude
mod TEST is
    pr UPLC-EXECUTOR .
endm
set print attribute on .

--- red generateMetaProgram("(program 1c.0.0 (con integer 16))") .
red processMetaProgram(generateMetaProgram("(program 1.0.0 (con integer 122))")) .
--- red uplc .
--- red parseTermToIntTerm(''16.Qid) .
q